{
  "$schema": "https://json.schemastore.org/claude-code-settings.json",
  "hooks": {
    "Notification": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "[ -n \"$NTFY_TOPIC\" ] && msg=$(jq -r '.message // \"Claude is waiting for input\"') && curl -fsS -X POST -H \"Title: Claude Code: needs approval\" -H \"Tags: bell\" -H \"Priority: high\" --data-binary \"$msg\" \"https://ntfy.sh/$NTFY_TOPIC\" >/dev/null 2>&1 || true",
            "async": true
          }
        ]
      }
    ]
  }
}
